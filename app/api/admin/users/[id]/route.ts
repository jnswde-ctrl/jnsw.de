import { activeAdminCount, updateUserAccess, type UserRole, type UserStatus } from "../../../../../db/users";
import { isActiveAdmin, publicUser, requireAppUser } from "../../../../auth";
const roles: UserRole[] = ["admin", "member"], statuses: UserStatus[] = ["active", "suspended"];
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const actor = await requireAppUser(); if (!actor) return Response.json({ error: "Authentication required" }, { status: 401 }); if (!isActiveAdmin(actor)) return Response.json({ error: "Administrator access required" }, { status: 403 }); const body = await request.json().catch(() => null) as { role?: unknown; status?: unknown } | null; const changes: { role?: UserRole; status?: UserStatus } = {}; if (typeof body?.role === "string" && roles.includes(body.role as UserRole)) changes.role = body.role as UserRole; if (typeof body?.status === "string" && statuses.includes(body.status as UserStatus)) changes.status = body.status as UserStatus; if (!changes.role && !changes.status) return Response.json({ error: "Provide a valid role or status" }, { status: 400 }); const { id } = await params; if (id === actor.id && (changes.role === "member" || changes.status === "suspended") && await activeAdminCount() <= 1) return Response.json({ error: "The last active administrator cannot remove their own access" }, { status: 409 }); const user = await updateUserAccess(id, changes); return user ? Response.json({ user: publicUser(user) }) : Response.json({ error: "User not found" }, { status: 404 }); }
