"use client";
import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
type Application={id:string;company:string;role:string;status:string;deadlineAt:string|null;followUpAt:string|null}; type Item={id:string;kind:string;title:string;organization:string|null}; type Suggestion={company:string;role:string;jobUrl:string|null;salary:string|null;deadlineAt:string|null;notes:string};
const labels:Record<string,string>={draft:"Entwurf",ready:"Bereit",sent:"Versendet",waiting:"Rückmeldung offen",interview:"Interview",offer:"Angebot",rejected:"Abgesagt",withdrawn:"Zurückgezogen",archived:"Archiviert"};
async function request(path:string,options?:RequestInit){const response=await fetch(path,{...options,headers:{"Content-Type":"application/json",...options?.headers}});const data=await response.json().catch(()=>null);if(!response.ok)throw new Error(data?.error??"Bitte erneut versuchen.");return data;}
export function Bewerbungswerkstatt(){const [applications,setApplications]=useState<Application[]>([]);const [items,setItems]=useState<Item[]>([]);const [message,setMessage]=useState("");const [step,setStep]=useState<"overview"|"application"|"profile">("overview");const [source,setSource]=useState("");const [mode,setMode]=useState<"url"|"text">("url");const [suggestion,setSuggestion]=useState<Suggestion|null>(null);const [loading,setLoading]=useState(false);
async function load(){try{const[a,p]=await Promise.all([request("/api/bewerbungswerkstatt/applications"),request("/api/bewerbungswerkstatt/profile")]);setApplications(a.applications);setItems(p.items);}catch(error){setMessage(error instanceof Error?error.message:"Laden fehlgeschlagen.");}}useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(timer);},[]);
async function submit(event:FormEvent<HTMLFormElement>,path:string,success:string){event.preventDefault();const form=event.currentTarget;try{await request(path,{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(form)))});form.reset();setMessage(success);await load();setStep("overview");setSuggestion(null);setSource("");}catch(error){setMessage(error instanceof Error?error.message:"Speichern fehlgeschlagen.");}}
async function importJob(event:FormEvent){event.preventDefault();setMessage("");setLoading(true);try{const result=await request("/api/bewerbungswerkstatt/import",{method:"POST",body:JSON.stringify(mode==="url"?{url:source}:{text:source})});setSuggestion(result.suggestion);}catch(error){setMessage(error instanceof Error?error.message:"Import fehlgeschlagen.");}finally{setLoading(false);}}
return <main className="workbench"><header className="workbench-head"><Link href="/community" className="workbench-back">← Community</Link><div><p className="eyebrow">Private Arbeitsfläche</p><h1>Bewerbungs<wbr/>werkstatt<span>.</span></h1></div><p>Deine Daten sind nur für dich sichtbar. Speichere nur Angaben, die für deine Bewerbung erforderlich sind.</p></header><nav className="workbench-steps" aria-label="Arbeitsbereiche">{(["overview","application","profile"] as const).map((name,index)=><button key={name} onClick={()=>setStep(name)} aria-current={step===name?"step":undefined}><b>0{index+1}</b>{name==="overview"?"Übersicht":name==="application"?"Bewerbung":"Profil"}</button>)}</nav>{message&&<p role="status" className="workbench-message">{message}</p>}{step==="overview"&&<section className="workbench-grid"><div><p className="eyebrow">Pipeline</p><h2>Deine Bewerbungen</h2><div className="application-list">{applications.length?applications.map(app=><article key={app.id}><div><Link href={`/community/bewerbungswerkstatt/${app.id}`}><b>{app.role}</b><span>{app.company} · {labels[app.status]}</span></Link>{app.deadlineAt&&<small>Frist: {app.deadlineAt}</small>}</div></article>):<p>Noch keine Bewerbung angelegt.</p>}</div></div><aside><p className="eyebrow">Nächste Schritte</p><h2>{applications.filter(app=>app.followUpAt).length} Follow-ups</h2><p>Fristen und Follow-ups bleiben pro Bewerbung dokumentiert.</p></aside></section>}{step==="application"&&<section className="workbench-form"><p className="eyebrow">Neue Bewerbung</p><h2>Stelle importieren.</h2><p className="workbench-intro">Lies eine öffentlich erreichbare Anzeige oder füge ihren Text ein. Die KI-Ausgabe ist ein Vorschlag: Prüfe und korrigiere alle Felder, bevor du speicherst. Der Originalinhalt wird nicht gespeichert.</p><form className="job-import-form" onSubmit={event=>void importJob(event)}><div className="import-tabs" role="group" aria-label="Importquelle"><button type="button" aria-pressed={mode==="url"} onClick={()=>setMode("url")}>URL lesen</button><button type="button" aria-pressed={mode==="text"} onClick={()=>setMode("text")}>Text einfügen</button></div><label className="workbench-wide">{mode==="url"?"URL der Stellenanzeige":"Beschreibungstext"}{mode==="url"?<input value={source} onChange={event=>setSource(event.target.value)} type="url" required placeholder="https://…" maxLength={2048}/>:<textarea value={source} onChange={event=>setSource(event.target.value)} required placeholder="Füge die öffentlich sichtbare Stellenbeschreibung hier ein …" maxLength={20000}/>}</label><button className="community-button" disabled={loading}>{loading?"Anzeige wird ausgewertet …":"Mit KI auswerten"}</button></form>{suggestion&&<form className="job-preview" onSubmit={event=>void submit(event,"/api/bewerbungswerkstatt/applications","Bewerbung gespeichert.")}><p className="eyebrow">KI-Vorschlag · vor dem Speichern prüfen</p><h2>Vorschau bearbeiten.</h2><label>Unternehmen<input name="company" required maxLength={160} defaultValue={suggestion.company}/></label><label>Stellenbezeichnung<input name="role" required maxLength={160} defaultValue={suggestion.role}/></label><label>Link zur Ausschreibung<input name="jobUrl" type="url" maxLength={2048} defaultValue={suggestion.jobUrl??""}/></label><label>Gehalt / Vergütung<input name="salary" maxLength={120} defaultValue={suggestion.salary??""}/></label><label>Frist<input name="deadlineAt" type="date" defaultValue={suggestion.deadlineAt??""}/></label><label>Status<select name="status"><option value="draft">Entwurf</option></select></label><label className="workbench-wide">Notizen<input name="notes" maxLength={8000} defaultValue={suggestion.notes}/></label><p className="workbench-wide import-notice">Erst mit „Bewerbung speichern“ wird ein privater Eintrag angelegt.</p><button className="community-button">Bewerbung speichern</button></form>}</section>}{step==="profile"&&<section className="workbench-form"><p className="eyebrow">Nachweise</p><h2>Erfahrung und Projekte.</h2><form onSubmit={event=>void submit(event,"/api/bewerbungswerkstatt/profile","Profilbaustein gespeichert.")}><label>Art<select name="kind"><option value="experience">Erfahrung</option><option value="project">Projekt</option></select></label><label>Titel<input name="title" required maxLength={160}/></label><label>Organisation<input name="organization" maxLength={160}/></label><label className="workbench-wide">Beschreibung<textarea name="description" maxLength={8000}/></label><button className="community-button">Baustein speichern</button></form><div className="profile-list">{items.map(item=><article key={item.id}><b>{item.title}</b><span>{item.kind}{item.organization&&` · ${item.organization}`}</span></article>)}</div></section>}</main>;}
