services:
  sitecreatorvinextstarter:
    image: sitecreatorvinextstarter
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 127.0.0.1:3000:3000
